import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MovieShowing = () => {
  return (
    <View>
      <Text>MovieShowing</Text>
    </View>
  );
};

export default MovieShowing;

const styles = StyleSheet.create({});
