import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MovieComing = () => {
  return (
    <View>
      <Text>MovieComing</Text>
    </View>
  );
};

export default MovieComing;

const styles = StyleSheet.create({});
